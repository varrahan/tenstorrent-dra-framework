# Package documentation is maintained in the project documentation rather than
# package-only doc.go files. User-facing validation errors intentionally begin
# with Kubernetes/API terms whose capitalization is part of the diagnostics contract.
checks = ["all", "-ST1000", "-ST1005"]
